{
  "name": "smooth-scroll-to-top",
  "version": "1.1.3",
  "description": "A React component that provides a smooth scrolling \"back to top\" button with customizable styles, colors, and position.",
  "keywords": [
    "smooth-scroll",
    "scroll-to-top",
    "react",
    "button",
    "scroll",
    "UI",
    "react-component"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Frank Riccardi",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.10",
    "@babel/preset-env": "^7.26.9",
    "@babel/preset-react": "^7.26.3"
  }
}
